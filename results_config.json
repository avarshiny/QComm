[    
    [
        {
            "output_type": "text",
            "title": "fidelity",
            "parameters": {
                "content": "{{ $.app_bob.fidelity }}"
            }
        }
    ]
]
